# =============================================================================
# FluxCode Multi-Host - Infra Env (PostgreSQL + Redis)
# =============================================================================
# 用途：配合 deploy/docker-compose.infra.yml 使用（数据库/缓存服务器）。
# 复制为 .env 后修改：
#   cp .env.infra.example .env
# =============================================================================

# Timezone
TZ=Asia/Shanghai

# -----------------------------------------------------------------------------
# PostgreSQL (REQUIRED)
# -----------------------------------------------------------------------------
POSTGRES_USER=fluxcode
POSTGRES_PASSWORD=change_this_secure_password
POSTGRES_DB=fluxcode

# 绑定到宿主机的地址/端口（建议绑定内网 IP，并用防火墙限制来源）
POSTGRES_BIND_HOST=0.0.0.0
POSTGRES_EXPOSE_PORT=5432

# -----------------------------------------------------------------------------
# Redis (REQUIRED in multi-host)
# -----------------------------------------------------------------------------
# 多机/跨主机部署时强烈建议启用密码（并用防火墙限制来源）
REDIS_PASSWORD=change_this_secure_redis_password

REDIS_BIND_HOST=0.0.0.0
REDIS_EXPOSE_PORT=6379

