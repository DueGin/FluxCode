# =============================================================================
# FluxCode Multi-Host - Caddy Env (entry node)
# =============================================================================
# 用途：配合 deploy/app/docker-compose.caddy.yml 使用（入口机）。
# 复制为 .env 后修改：
#   cp .env.caddy.example .env
# =============================================================================

# ----------------------------------------------------------------------------
# Ingress (Caddy)
# ----------------------------------------------------------------------------
# 绑定地址（一般保持 0.0.0.0）
BIND_HOST=0.0.0.0

# 对外端口（默认 80/443）
HTTP_PORT=80
HTTPS_PORT=443

# 域名（推荐）：设置为你的域名，例如 flux-code.cc
# 留空则仅提供 HTTP（默认监听 :80，不会自动签发 HTTPS 证书）
CADDY_ADDRESS=flux-code.cc

# 时区
TZ=Asia/Shanghai

# ----------------------------------------------------------------------------
# Docker logging
# ----------------------------------------------------------------------------
DOCKER_LOG_MAX_SIZE=20m
DOCKER_LOG_MAX_FILE=5
